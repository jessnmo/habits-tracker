//import { useState } from 'react';

/* const HabitHeader = ({ value }) => {
	//const [headerValue, setHeaderValue] = useState(value);
	return (
		<>
			<h1>{value}</h1>
		</>
	);
};

export default HabitHeader; */
